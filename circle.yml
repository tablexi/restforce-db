machine:
  ruby:
    version: 2.2.2
dependencies:
  pre:
    # Get the most recent bundler
    - gem install bundler
database:
  override:
    # Do nothing; we don't need any database setup.
    - echo "SKIPPING"
test:
  override:
    - bundle exec rubocop
    - bundle exec rake test
